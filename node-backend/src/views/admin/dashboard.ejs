<nav class="col-md-2 d-md-block sidebar">
    <div class="text-center mb-4">
        <img src="/assets/img/pagina/logo.svg" alt="Logo" class="img-fluid"
            style="max-height: 60px; border-radius: 50%;">
        <h5 class="mt-2 fw-bold" style="color: var(--primary-color);">Admin Panel</h5>
    </div>
    <hr style="border-color: rgba(255,255,255,0.1);">

    <a href="/" target="_blank">
        <i class="bi bi-globe"></i> Ver mi Web
    </a>



    <a href="/admin/dynamic-sections">
        <i class="bi bi-puzzle"></i> Mis Secciones
    </a>

    <% if (role==='admin' ) { %>
        <a href="/admin/users">
            <i class="bi bi-people"></i> Usuarios
        </a>
        <% } %>

            <div class="mt-auto">
                <hr style="border-color: rgba(255,255,255,0.1);">
                <a href="/admin/logout" class="text-danger">
                    <i class="bi bi-box-arrow-right"></i> Cerrar Sesión
                </a>
                <div class="mt-3 small text-white-50 p-2">
                    <i class="bi bi-person-circle"></i>
                    <%= username %> <br>
                        <span class="badge bg-secondary mt-1">
                            <%= role %>
                        </span>
                </div>
            </div>
</nav>

<main class="col-md-10 p-4">
    <div class="d-flex justify-content-between align-items-center pt-3 pb-2 mb-4 border-bottom">
        <h1 class="h2">Panel de Control</h1>
    </div>

    <!-- Tabs for Sections -->
    <ul class="nav nav-pills mb-4" id="pills-tab" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="pills-services-tab" data-bs-toggle="pill"
                data-bs-target="#pills-services" type="button" role="tab">📖 Parashot</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="pills-portfolio-tab" data-bs-toggle="pill" data-bs-target="#pills-portfolio"
                type="button" role="tab">🖼️ Eventos</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="pills-team-tab" data-bs-toggle="pill" data-bs-target="#pills-team"
                type="button" role="tab">👥 Equipo</button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="pills-testimonials-tab" data-bs-toggle="pill"
                data-bs-target="#pills-testimonials" type="button" role="tab">⭐ Testimonios</button>
        </li>

    </ul>

    <div class="tab-content" id="pills-tabContent">
        <!-- PARASHOT -->
        <div class="tab-pane fade show active" id="pills-services" role="tabpanel">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="mb-0">Gestión de Parashot</h4>
                <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#createParashaModal">
                    <i class="bi bi-plus-circle"></i> Nueva Parasha
                </button>
            </div>

            <div class="table-responsive">
                <table class="table table-dark table-hover">
                    <thead>
                        <tr>
                            <th>Nº</th>
                            <th>Título</th>
                            <th>Descripción</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% parashot.forEach(p=> { %>
                            <tr>
                                <td>
                                    <%= p.parasha_number || '-' %>
                                </td>
                                <td>
                                    <%= p.title %>
                                </td>
                                <td>
                                    <%= p.description %>
                                </td>
                                <td>
                                    <a href="/admin/parashot/edit/<%= p.id %>" class="btn btn-sm btn-primary me-2">Editar</a>
                                    <a href="/admin/parashot/delete/<%= p.id %>" class="btn btn-sm btn-danger"
                                        onclick="return confirm('¿Eliminar?')">Eliminar</a>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- PORTFOLIO (EVENTOS) -->
        <div class="tab-pane fade" id="pills-portfolio" role="tabpanel">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="mb-0">Gestión de Eventos</h4>
                <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#createPortfolioModal">
                    <i class="bi bi-plus-circle"></i> Nuevo Evento
                </button>
            </div>

            <div class="table-responsive">
                <table class="table table-dark table-hover">
                    <thead>
                        <tr>
                            <th>Título</th>
                            <th>Categoría</th>
                            <th>Fecha</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% portfolio.forEach(item=> { %>
                            <tr>
                                <td><%= item.title %></td>
                                <td><%= item.category %></td>
                                <td><%= item.event_date ? new Date(item.event_date).toLocaleDateString() : '-' %></td>
                                <td>
                                    <a href="/admin/portfolio/edit/<%= item.id %>" class="btn btn-sm btn-primary me-2">Editar</a>
                                    <a href="/admin/portfolio/delete/<%= item.id %>" class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar?')">Eliminar</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- TEAM (EQUIPO) -->
        <div class="tab-pane fade" id="pills-team" role="tabpanel">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="mb-0">Gestión de Equipo</h4>
                <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#createTeamModal">
                    <i class="bi bi-plus-circle"></i> Nuevo Miembro
                </button>
            </div>

            <div class="table-responsive">
                <table class="table table-dark table-hover">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Rol / Cargo</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% team.forEach(member=> { %>
                            <tr>
                                <td><%= member.name %></td>
                                <td><%= member.role %></td>
                                <td>
                                    <a href="/admin/team/delete/<%= member.id %>" class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar?')">Eliminar</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>

        <!-- TESTIMONIALS -->
        <div class="tab-pane fade" id="pills-testimonials" role="tabpanel">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h4 class="mb-0">Gestión de Testimonios</h4>
                <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#createTestimonialModal">
                    <i class="bi bi-plus-circle"></i> Nuevo Testimonio
                </button>
            </div>

            <div class="table-responsive">
                <table class="table table-dark table-hover">
                    <thead>
                        <tr>
                            <th>Nombre</th>
                            <th>Rol</th>
                            <th>Testimonio</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% testimonials.forEach(t=> { %>
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <img src="<%= t.img %>" style="width: 30px; height: 30px; border-radius: 50%;" class="me-2">
                                        <%= t.name %>
                                    </div>
                                </td>
                                <td><%= t.role %></td>
                                <td><span class="text-truncate d-inline-block" style="max-width: 300px;"><%= t.text %></span></td>
                                <td>
                                    <a href="/admin/testimonials/delete/<%= t.id %>" class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar?')">Eliminar</a>
                                </td>
                            </tr>
                        <% }) %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</main>

<!-- MODAL: NUEVA PARASHA -->
<div class="modal fade" id="createParashaModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content bg-dark text-white border-secondary">
            <div class="modal-header border-secondary">
                <h5 class="modal-title">Añadir Nueva Parasha</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="/admin/parashot/create" method="POST" enctype="multipart/form-data" id="createParashaForm">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-2">
                            <label class="form-label small text-white-50">Nº</label>
                            <input type="number" name="parasha_number" class="form-control bg-secondary text-white border-0" placeholder="#" min="1">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Título</label>
                            <input type="text" name="title" class="form-control bg-secondary text-white border-0" required placeholder="Ej: Bereshit">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Subtítulo / Versículo</label>
                            <input type="text" name="subtitle" class="form-control bg-secondary text-white border-0" placeholder="Ej: Génesis 1:1">
                        </div>
                        
                        <div class="col-md-12">
                            <label class="form-label small text-white-50">Resumen Breve</label>
                            <input type="text" name="description" class="form-control bg-secondary text-white border-0" placeholder="Introducción corta...">
                        </div>

                        <div class="col-md-12">
                            <label class="form-label small text-white-50">Link de YouTube</label>
                            <div class="input-group">
                                <span class="input-group-text bg-secondary text-white border-0"><i class="bi bi-youtube"></i></span>
                                <input type="text" name="youtube_link" class="form-control bg-secondary text-white border-0" placeholder="https://www.youtube.com/watch?v=...">
                            </div>
                        </div>

                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Subir Imagen</label>
                            <input type="file" id="create_image_file" name="image_file" class="form-control bg-secondary text-white border-0">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">O URL de Imagen</label>
                            <input type="text" id="create_image_url" name="image_url" class="form-control bg-secondary text-white border-0" placeholder="https://...">
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="border border-secondary rounded overflow-hidden d-flex align-items-center justify-content-center" style="height: 100px; background: rgba(0,0,0,0.2);">
                                <img id="createImgPreview" src="/assets/img/blog/blog-1.jpg" style="max-width: 100%; max-height: 100%; object-fit: contain;">
                            </div>
                        </div>

                        <div class="col-12">
                            <label class="form-label small text-white-50">Contenido Completo (Enseñanza)</label>
                            <div id="create-editor-container" style="height: 300px; background: white; color: black; border-radius: 5px;"></div>
                            <input type="hidden" name="content" id="createContentInput">
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-secondary">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-success px-4">Crear Parasha</button>
                </div>
            </form>
        </div>
    </div>
</div>

<div class="modal fade" id="createPortfolioModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-xl modal-dialog-centered">
        <div class="modal-content bg-dark text-white border-secondary">
            <div class="modal-header border-secondary">
                <h5 class="modal-title">Añadir Nuevo Evento</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="/admin/portfolio/create" method="POST" enctype="multipart/form-data" id="createPortfolioForm">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Título</label>
                            <input type="text" name="title" class="form-control bg-secondary text-white border-0" required placeholder="Ej: Pesaj 2026">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Categoría</label>
                            <select name="category" class="form-select bg-secondary text-white border-0">
                                <option value="filter-app">App</option>
                                <option value="filter-product">Producto</option>
                                <option value="filter-branding">Marca</option>
                            </select>
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Fecha</label>
                            <input type="date" name="event_date" class="form-control bg-secondary text-white border-0">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Subtítulo</label>
                            <input type="text" name="subtitle" class="form-control bg-secondary text-white border-0" placeholder="Ej: Celebración anual">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Descripción Breve</label>
                            <input type="text" name="description" class="form-control bg-secondary text-white border-0" placeholder="Resumen para la lista">
                        </div>

                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Subir Imagen</label>
                            <input type="file" id="port_image_file" name="image_file" class="form-control bg-secondary text-white border-0">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">O URL de Imagen</label>
                            <input type="text" id="port_image_url" name="image_url" class="form-control bg-secondary text-white border-0" placeholder="https://...">
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="border border-secondary rounded overflow-hidden d-flex align-items-center justify-content-center" style="height: 100px; background: rgba(0,0,0,0.2);">
                                <img id="portImgPreview" src="/assets/img/blog/blog-1.jpg" style="max-width: 100%; max-height: 100%; object-fit: contain;">
                            </div>
                        </div>

                        <div class="col-12">
                            <label class="form-label small text-white-50">Detalle del Evento (Contenido)</label>
                            <div id="port-editor-container" style="height: 300px; background: white; color: black; border-radius: 5px;"></div>
                            <input type="hidden" name="content" id="portContentInput">
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-secondary">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-success px-4">Crear Evento</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- MODAL: NUEVO MIEMBRO -->
<div class="modal fade" id="createTeamModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content bg-dark text-white border-secondary">
            <div class="modal-header border-secondary">
                <h5 class="modal-title">Añadir Miembro del Equipo</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="/admin/team/create" method="POST" enctype="multipart/form-data" id="createTeamForm">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Nombre</label>
                            <input type="text" name="name" class="form-control bg-secondary text-white border-0" required placeholder="Nombre completo">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Rol / Cargo</label>
                            <input type="text" name="role" class="form-control bg-secondary text-white border-0" required placeholder="Director, Maestro, etc.">
                        </div>
                        <div class="col-md-12">
                            <label class="form-label small text-white-50">Descripción / Bio</label>
                            <textarea name="description" class="form-control bg-secondary text-white border-0" rows="3" placeholder="Breve biografía..."></textarea>
                        </div>

                        <div class="col-md-4">
                            <label class="form-label small text-white-50">Subir Foto</label>
                            <input type="file" id="team_image_file" name="image_file" class="form-control bg-secondary text-white border-0">
                        </div>
                        <div class="col-md-4">
                            <label class="form-label small text-white-50">O URL de Foto</label>
                            <input type="text" id="team_image_url" name="img" class="form-control bg-secondary text-white border-0" placeholder="https://...">
                        </div>
                        <div class="col-md-4 text-center">
                            <div class="border border-secondary rounded overflow-hidden d-flex align-items-center justify-content-center" style="height: 120px; width: 120px; border-radius: 50%; margin: 0 auto; background: rgba(0,0,0,0.2);">
                                <img id="teamImgPreview" src="/assets/img/team/team-1.jpg" style="width: 100%; height: 100%; object-fit: cover;">
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-secondary">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-success px-4">Añadir Miembro</button>
                </div>
            </form>
        </div>
    </div>
</div>


<!-- MODAL: NUEVO TESTIMONIO -->
<div class="modal fade" id="createTestimonialModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content bg-dark text-white border-secondary">
            <div class="modal-header border-secondary">
                <h5 class="modal-title">Añadir Testimonio</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form action="/admin/testimonials/create" method="POST">
                <div class="modal-body">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Nombre</label>
                            <input type="text" name="name" class="form-control bg-secondary text-white border-0" required placeholder="Ej: Juan Pérez">
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small text-white-50">Rol / Cargo</label>
                            <input type="text" name="role" class="form-control bg-secondary text-white border-0" required placeholder="Ej: Estudiante">
                        </div>
                        <div class="col-md-12">
                            <label class="form-label small text-white-50">URL de Imagen (Avatar)</label>
                            <input type="text" name="img" class="form-control bg-secondary text-white border-0" required placeholder="https://...">
                        </div>
                        <div class="col-md-12">
                            <label class="form-label small text-white-50">Testimonio</label>
                            <textarea name="text" class="form-control bg-secondary text-white border-0" rows="3" required placeholder="Escribe el testimonio aquí..."></textarea>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-secondary">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-success px-4">Añadir Testimonio</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Quill -->
<link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>

<script>
    document.addEventListener('DOMContentLoaded', function() {
        // --- QUILL EDITORS ---
        const quillConfig = {
            theme: 'snow',
            modules: {
                toolbar: [
                    [{ 'header': [1, 2, 3, false] }],
                    ['bold', 'italic', 'underline', 'strike'],
                    [{ 'color': [] }, { 'background': [] }],
                    ['link', 'blockquote', 'code-block', 'image'],
                    [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                    [{ 'align': [] }],
                    ['clean']
                ]
            }
        };

        const quillParasha = new Quill('#create-editor-container', quillConfig);
        const quillPortfolio = new Quill('#port-editor-container', quillConfig);

        // --- IMAGE PREVIEWS ---
        function setupPreview(inputId, fileId, previewId, defaultImg) {
            const textInput = document.getElementById(inputId);
            const fileInput = document.getElementById(fileId);
            const preview = document.getElementById(previewId);

            if (textInput) {
                textInput.addEventListener('input', (e) => {
                    if (e.target.value) preview.src = e.target.value;
                    else preview.src = defaultImg;
                });
            }

            if (fileInput) {
                fileInput.addEventListener('change', function() {
                    const file = this.files[0];
                    if (file) {
                        const reader = new FileReader();
                        reader.onload = function(e) { preview.src = e.target.result; }
                        reader.readAsDataURL(file);
                    }
                });
            }
        }

        setupPreview('create_image_url', 'create_image_file', 'createImgPreview', '/assets/img/blog/blog-1.jpg');
        setupPreview('port_image_url', 'port_image_file', 'portImgPreview', '/assets/img/blog/blog-1.jpg');
        setupPreview('team_image_url', 'team_image_file', 'teamImgPreview', '/assets/img/team/team-1.jpg');

        // --- FORM SUBMISSIONS ---
        document.getElementById('createParashaForm').onsubmit = function() {
            document.getElementById('createContentInput').value = quillParasha.root.innerHTML;
            return true;
        };

        document.getElementById('createPortfolioForm').onsubmit = function() {
            document.getElementById('portContentInput').value = quillPortfolio.root.innerHTML;
            return true;
        };

        // --- TABS & HASH ---
        const hash = window.location.hash;
        if (hash) {
            const triggerEl = document.querySelector(`.nav-pills button[data-bs-target="${hash}"]`);
            if (triggerEl) bootstrap.Tab.getOrCreateInstance(triggerEl).show();
        }
        
        document.querySelectorAll('.nav-pills button').forEach(button => {
            button.addEventListener('shown.bs.tab', (e) => {
                window.location.hash = e.target.getAttribute('data-bs-target');
            });
        });
    });
</script>